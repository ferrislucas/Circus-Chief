# PostHog Analytics — baked into the frontend bundle at build time.
# This is a public client-side key (not a secret).
# Leave empty to disable analytics in the built bundle.
VITE_POSTHOG_KEY=phc_your_actual_key_here
VITE_POSTHOG_HOST=https://us.i.posthog.com
